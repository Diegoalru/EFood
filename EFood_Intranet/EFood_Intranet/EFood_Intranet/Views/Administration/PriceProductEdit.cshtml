@model EFoodBLL.IntranetModels.ReturnPrice

@{
    ViewBag.Title = "Edicion de Producto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="home">
    <div class="container">
        <br>
        <div class="row omb_row-sm-offset-3 omb_socialButtons">
        </div>
        @using (Html.BeginForm("PriceProductEdit", "Administration", new{ precioId = @Model.PkCode }, FormMethod.Post, new {role = "form", @class = "center3"}))
        {
            <h4 style="color: orangered;">Información del Producto</h4>
            @Html.ValidationSummary("", new {@class = "ErrorMessage"})
            <label><stong>Tipo de Precio:</stong> @Model.Type</label>
            <br/>
            <label><stong>Monto:</stong></label>
            <br/>
            @Html.TextBoxFor(item => item.Amount)
            <br/>
            <input type="submit" class="primary-btn" value="Guardar"/>
            @Html.ActionLink("Volver", "PriceProductList", new {id = Model.Product})
        }
    </div>
</div>